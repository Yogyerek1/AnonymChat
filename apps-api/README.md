
Create and activate a virtual environment:
- python3 -m venv venv
- source venv/bin/activate

Install packages run this command: pip install -r requirements.txt

Start mongodb server:
- sudo service mongod start

Run the API:
- python3 api.py